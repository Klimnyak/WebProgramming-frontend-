FROM node:18

# Встановлюємо глобально пакет serve
RUN npm install -g serve

# Встановлюємо робочу директорію
WORKDIR /app

# Копіюємо файли в контейнер
COPY . .

# Відкриваємо порт 8080
EXPOSE 8080

# Запускаємо serve для обслуговування статичних файлів
CMD ["serve", "-s", ".", "-l", "8080"]
